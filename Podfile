# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'CraftedMusicBox' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for CraftedMusicBox
  pod 'SVProgressHUD', '~> 2.3'
  pod 'Reachability', '~> 3.7'
  # pod 'SWTableViewCell', '~> 0.3'  # Removed - abandoned library, replacing with native iOS swipe actions
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  pod 'Firebase/AdMob'
  pod 'Fabric', '~> 1.10.2'
  pod 'Crashlytics', '~> 3.14.0'
  pod 'Firebase/Analytics'

  target 'CraftedMusicBoxTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
